#include "input.h"

// read input
void readInput(void) {
    scanf("%lf", &L);
    scanf("%d", &N);
    scanf("%lf", &temp);
    scanf("%lf", &dt);
    scanf("%d", &bstep);
    scanf("%d", &nstep);
    scanf("%d", &cpstep);
    return;
}